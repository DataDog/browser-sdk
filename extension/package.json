{
  "name": "extension",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/chrome": "0.0.120",
    "@types/node": "14.0.23",
    "ts-loader": "8.0.1",
    "tsconfig-paths-webpack-plugin": "3.2.0",
    "tslint": "6.1.2",
    "typescript": "3.9.6",
    "webpack": "4.43.0",
    "webpack-cli": "3.3.12"
  },
  "scripts": {
    "clean": "rm -rf dist && mkdir dist && cp manifest.json dist && cp -r src/assets dist/",
    "build": "yarn clean && webpack --config webpack.config.js",
    "watch": "yarn clean && webpack --config webpack.config.js -w"
  }
}
